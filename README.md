# sensmaps

Interactive GUI for 2D sensitivity maps in diffuse optical imaging.

## Development

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
```

Run the GUI:
```bash
sensmaps
```

Run tests:
```bash
pytest
```
